<Window x:Class="MVVM.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM.View"
        xmlns:ViewModel="clr-namespace:MVVM.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=ViewModel:MainViewModel}"
        Title="Test" Height="500" Width="550" MinHeight="450" MinWidth="400" WindowStyle="None">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.08*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Height="35" VerticalAlignment="Top" HorizontalAlignment="Center">
            <Button Command="{Binding Button1Command}" Name="Button1" Background="LightPink" Content="Button1" MinWidth="60"/>
            <Button Command="{Binding Button2Command}" Name="Button2" Background="LightPink" Content="Button2" MinWidth="60" Margin="20,0,0,0"/>
            <Button Command="{Binding Button3Command}" Name="Button3" Background="LightPink" Content="Button3" MinWidth="60" Margin="20,0,0,0"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>

    </Grid>
    
</Window>

